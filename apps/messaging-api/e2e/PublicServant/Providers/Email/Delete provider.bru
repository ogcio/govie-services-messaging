meta {
  name: Delete provider
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/api/v1/providers/{{emailProviderId}}?type=email
  body: none
  auth: bearer
}

query {
  type: email
}

auth:bearer {
  token: {{accessToken}}
}
